
  # Shoe store

  This is a code bundle for Shoe store. The original project is available at https://www.figma.com/design/uAv864u79s5ZaI3CfummXZ/Shoe-store.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  